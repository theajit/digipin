# Pin Code Cafe Website

Brand website for Pin Code Cafe 759001.

```bash
npm install
npm run dev
```

Production uses `npm run build` and `npm start` on port `3000`.

This site lives on the independent `pincode-cafe-website` branch. It does not share the DIGIPIN UI branch or its canonical metadata.
